import { Showcase } from "@/components/showcase/Showcase";

/**
 * /showcase — Server Component entry. Renders the single client shell that owns
 * the whole composition tree (providers, SmoothScroll, all 13 technique demos).
 */
export default function ShowcasePage() {
  return <Showcase />;
}
