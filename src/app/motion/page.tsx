import { MotionComparison } from '@/components/motion/MotionComparison';

/**
 * /motion — Server Component entry. Renders the single client shell that owns
 * the comparison (one fluid-distortion treatment, three editorial photos).
 */
export default function MotionPage() {
  return <MotionComparison />;
}
